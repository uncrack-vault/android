<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/homeFragment"
        app:iconTint="@color/black"
        android:icon="@drawable/home_icon"
        android:title="Home"/>

    <item android:id="@+id/cardFragment"
        app:iconTint="@color/black"
        android:icon="@drawable/credit_card"
        android:title="Card"/>

    <item android:id="@+id/passwordFragment"
        app:iconTint="@color/black"
        android:icon="@drawable/generate_password"
        android:title="Generate Password"/>

    <item android:id="@+id/settingsFragment"
        app:iconTint="@color/black"
        android:icon="@drawable/setting"
        android:title="Settings"/>

</menu>